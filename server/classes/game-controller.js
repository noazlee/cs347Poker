const Game = require('./game-model');
const Table = require('./table');
const Player = require('./player');

class GameController {
    constructor() {
        this.games = {}; //stores all games
    }
}